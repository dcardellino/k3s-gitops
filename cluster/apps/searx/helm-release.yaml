---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: searx
  namespace: searx
spec:
  interval: 1h
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: searx
      version: 5.4.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home-charts
        namespace: flux-system
  values:
    searx:
      baseUrl: https://roogle.domrocco.de
      mortyKey: ofR0HuwEFLi9y6NaqklznObRWmnXiY2UFMomCoI78xRsFEj9Fj
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
          external-dns/is-public: "true"
          external-dns.alpha.kubernetes.io/target: "homelab-tunnel.domrocco.de"
          external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
        hosts:
          - host: roogle.domrocco.de
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: searx-server-tls
            hosts:
              - roogle.domrocco.de
